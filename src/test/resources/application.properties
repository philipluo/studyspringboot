jobs.fixedDelay=5000  
jobs.cron=0/5 * *  * * ?  


#当在此处按照这些固定名字设置以后，不需要做任何其他设置，在没有其他Data configuration的情况下，springboot将自动识别各个entity和repository，也不需要手工创建任何entityManagerFactory或者transactionManager，entity还是需要被注解@Entity，但repository不需要被注解。如果系统里面但凡有一个Data configuration，将不会是使用defaultEntityManagerFactory了。
spring.datasource.url=jdbc:mysql://104.155.201.198:8306/mysql
spring.datasource.username=root
spring.datasource.password=philippwd
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQL5Dialect

spring.jpa.show-sql=true
spring.jpa.hibernate.use-new-id-generator-mappings=false
